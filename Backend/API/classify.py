from .pipeline.pipeline_preparation import pipeline_file_from_img, pipeline_file_from_PDF

# Process here the document

"""
??? Check if we pu data in the pipeline WHILE going in the model or BEFORE ???
"""

# def make_classifying(input_data, model_path=MODEL_PATH):
    # if input_data == an image:
        # put data in the pipeline_file_from_img pipeline
    # else:
        # pipeline_file_from_PDF

